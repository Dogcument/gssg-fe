import * as React from 'react';
import { View, Text } from 'react-native';

function WritingScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Writing Screen</Text>
      </View>
    );
  }
  
export default WritingScreen; 
