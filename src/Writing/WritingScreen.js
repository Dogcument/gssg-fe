import * as React from 'react';
import { View, Text } from 'react-native';

function WritingScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>실제로 작성하는 곳</Text>
      </View>
    );
  }
  
export default WritingScreen; 
